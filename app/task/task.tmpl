 {{#data}}
<li>
  <a class="list-item" href="#" taskid="{{id}}" fid="{{fid}}" status="{{status}}" wtype="{{wtype}}">
    <span class="content">{{title}}</span>
    <span>{{date}}</span>
  </a>
</li>
{{/data}}
