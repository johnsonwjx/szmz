 {{#data}}
<li>
  <a href="#" taskid="{{id}}" fid="{{fid}}" status="{{status}}" wtype="{{wtype}}">
    <span class="content">{{title}}</span>
    <span class="right">{{date}}</span>
  </a>
</li>
{{/data}}
