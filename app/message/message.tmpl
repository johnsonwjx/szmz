<ul>
  {{#tasks}}
  <li>
    <a class="list-item" href="#" taskid="{{id}}" fid="{{fid}}" status="{{status}}" wtype="{{wtype}}">
      <span class="content">
        {{title}}<span class="right">{{date}}</span>
      </span>

    </a>
  </li>
  {{/tasks}}
</ul>
